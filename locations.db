CREATE TABLE locations (id INTEGER, city VARCHAR(20), state VARCHAR(20)) PRIMARY KEY (id);
INSERT INTO locations VALUES FROM (1, "College Station", "Texas");
INSERT INTO locations VALUES FROM (2, "Bryan", "Texas");
INSERT INTO locations VALUES FROM (3, "Houston", "Texas");
